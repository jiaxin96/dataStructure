#include <cstdio>
#include <vector>
#include <list>
using namespace std;

int main(int argc, char const *argv[])
{
	int t;
	scanf("%d", &t);
	while (t--) {
		int n, m;
		scanf("%d%d", &n, &m);
		vector< list<int> > mm(n);
		int p1,p2;
		for (size_t i = 0; i < m; i++)
		{
			scanf("%d%d", &p1, &p2);
			
		}
	}
	return 0;
}